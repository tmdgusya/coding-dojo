[project]
name = "rate-limiting-dojo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=9.0.2",
    "typed-pytest>=0.1.8",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
